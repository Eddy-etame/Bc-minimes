<!doctype html>
<html lang="fr" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Activités & disciplines — Boxing Center Minimes, Toulouse (boxe anglaise, éducative dès 3 ans, Lady)</title>
  <meta name="description" content="Toutes les disciplines de la salle des Minimes (Toulouse 31200) : boxe anglaise loisirs et compétiteurs, boxe éducative dès 3 ans (Baby Boxe 3/6), Boxing Lady, PAOS, cross training, cardio boxing. Un écran par discipline, séance d'essai 10€." />
  <meta name="keywords" content="disciplines boxe Minimes Toulouse, boxe anglaise Toulouse, boxe éducative enfant, Boxing Lady Toulouse, cross training boxe, cardio boxing 31200, cours de boxe Barrière de Paris" />
  <meta name="robots" content="index, follow, max-image-preview:large" />
  <link rel="canonical" href="https://minimes.boxingcenter.fr/activites/" />
  <meta name="theme-color" content="#0a0908" />
  <link rel="icon" href="/assets/favicon.svg" type="image/svg+xml" />
  <meta name="geo.region" content="FR-31" />
  <meta name="geo.placename" content="Toulouse — Les Minimes" />
  <meta name="geo.position" content="43.6256;1.4309" />
  <meta name="ICBM" content="43.6256, 1.4309" />

  <!-- Open Graph -->
  <meta property="og:type" content="website" />
  <meta property="og:locale" content="fr_FR" />
  <meta property="og:site_name" content="Boxing Center Minimes" />
  <meta property="og:title" content="Activités & disciplines — Boxing Center Minimes, Toulouse" />
  <meta property="og:description" content="Boxe anglaise, éducative dès 3 ans, Boxing Lady, PAOS, cross training, cardio boxing. Un écran par discipline, essai 10€." />
  <meta property="og:url" content="https://minimes.boxingcenter.fr/activites/" />
  <meta property="og:image" content="https://minimes.boxingcenter.fr/assets/img/og.png" />
  <meta property="og:image:alt" content="Les disciplines de Boxing Center Minimes — boxe anglaise, éducative, Lady, technique." />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="Activités & disciplines — Boxing Center Minimes, Toulouse" />
  <meta name="twitter:description" content="Boxe anglaise, éducative dès 3 ans, Boxing Lady, PAOS, cross, cardio boxing. Un écran par discipline, essai 10€." />
  <meta name="twitter:image" content="https://minimes.boxingcenter.fr/assets/img/og.png" />

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Archivo+Black&family=Archivo:wght@400;500;600;700;800&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet" />

  <!-- LCP : le hero est construit en JS → invisible au preload scanner (§5.8) -->
  <link rel="preload" as="image" href="/assets/img/bc/training-2.webp" fetchpriority="high" />

  <link rel="stylesheet" href="/assets/css/base.css?v=b5" />
  <link rel="stylesheet" href="/assets/css/home.css?v=b5" />

  <script is:inline type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Accueil", "item": "https://minimes.boxingcenter.fr/" },
      { "@type": "ListItem", "position": 2, "name": "Activités", "item": "https://minimes.boxingcenter.fr/activites/" }
    ]
  }
  </script>
  <script is:inline type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "ItemList",
    "name": "Disciplines — Boxing Center Minimes",
    "url": "https://minimes.boxingcenter.fr/activites/",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Boxe anglaise", "url": "https://minimes.boxingcenter.fr/activites/#anglaise" },
      { "@type": "ListItem", "position": 2, "name": "Boxe éducative (dès 3 ans)", "url": "https://minimes.boxingcenter.fr/activites/#educative" },
      { "@type": "ListItem", "position": 3, "name": "Boxing Lady", "url": "https://minimes.boxingcenter.fr/activites/#lady" },
      { "@type": "ListItem", "position": 4, "name": "PAOS & pattes d'ours", "url": "https://minimes.boxingcenter.fr/activites/#paos" },
      { "@type": "ListItem", "position": 5, "name": "Cross training", "url": "https://minimes.boxingcenter.fr/activites/#cross" },
      { "@type": "ListItem", "position": 6, "name": "Cardio boxing", "url": "https://minimes.boxingcenter.fr/activites/#cardio" }
    ]
  }
  </script>

  <!-- page-scoped : sommaire ancré + rangées disciplines (un écran/discipline,
       jamais une grille de carrés — c'est un catalogue qui se déroule) -->
  <style is:inline>
    /* l'en-tête partage .page-head (base.css) — la répétition est honnête.
       Ce qui appartient à CETTE page : le sommaire ancré, qui colle sous la
       nav et suit la lecture. */
    /* sommaire — chips ancrées, colle sous la nav au scroll */
    .act-index { position: sticky; top: 0; z-index: 20; display: flex; flex-wrap: wrap; gap: .5rem; padding: clamp(.8rem,2vw,1.1rem) 0; background: rgba(10,9,8,.82); backdrop-filter: blur(10px); border-bottom: 1px solid var(--line); }
    .act-chip { display: inline-flex; align-items: center; gap: .7ch; min-height: 44px; padding: .55em .95em; border: 1px solid var(--line); font: 700 var(--step--1)/1 var(--f-mono); letter-spacing: .06em; text-transform: uppercase; color: var(--bone-dim); transition: color .3s, border-color .3s, background .3s; }
    .act-chip__n { color: var(--bone-dim); }
    .act-chip:hover, .act-chip:active { color: var(--ink); background: var(--bone); border-color: var(--bone); }
    .act-chip:hover .act-chip__n, .act-chip:active .act-chip__n { color: var(--bone-dim); }

    /* rangée discipline */
    .act-rows { border-top: 0; }
    .act { position: relative; display: grid; grid-template-columns: 1fr; gap: clamp(1.6rem,4vw,3.5rem); align-items: center; padding-block: clamp(3rem,7vw,5.5rem); border-bottom: 1px solid var(--line); scroll-margin-top: 5.5rem; }
    .act > * { min-width: 0; }   /* grid children must be shrinkable (no overflow) */
    @media (min-width: 900px){ .act { grid-template-columns: 1.05fr 1fr; } .act--rev .act__media { order: 2; } }
    /* align-self:start — NOT stretch: a stretched (tall) height + aspect-ratio
       would derive a width wider than the column → 1280px horizontal scroll */
    .act__media { position: relative; aspect-ratio: 4/3; align-self: start; min-height: 15rem; }
    .act__media img { filter: grayscale(1) contrast(1.2) brightness(.8); object-position: 50% 30%; transition: filter .6s var(--ease), transform 1s var(--ease-out); transform: scale(1.03); }
    .act:hover .act__media img, .act:active .act__media img { filter: grayscale(1) contrast(1.1) brightness(.95); transform: scale(1); }
    .act__body { position: relative; }
    .act__bigidx { position: absolute; top: -2.4rem; right: 0; font-family: var(--f-display); font-size: clamp(4rem,11vw,9rem); line-height: .8; color: transparent; -webkit-text-stroke: 1.5px var(--line); pointer-events: none; z-index: 0; }
    .act__tag { position: relative; z-index: 1; display: inline-flex; align-items: center; gap: .7ch; font: 700 var(--step--1)/1 var(--f-mono); letter-spacing: .16em; text-transform: uppercase; color: var(--bone); }
    .act__tag::before { content: ""; width: 18px; height: 2px; background: var(--red); flex: 0 0 18px; }
    .act__name { position: relative; z-index: 1; font-family: var(--f-display); text-transform: uppercase; font-size: clamp(2rem,5.5vw,4rem); line-height: .92; margin: .7rem 0 1rem; color: var(--bone); }
    .act__desc { position: relative; z-index: 1; font-size: var(--step-1); line-height: 1.4; color: var(--bone); font-weight: 500; max-width: 46ch; }
    .act__for { position: relative; z-index: 1; display: flex; flex-wrap: wrap; align-items: baseline; gap: .5rem 1ch; margin: 1.4rem 0 1rem; color: var(--read); font-size: var(--step-0); }
    .act__for span { font: 700 var(--step--1)/1 var(--f-mono); letter-spacing: .14em; text-transform: uppercase; color: var(--bone); border-left: 2px solid var(--red); padding-left: .7ch; }
    .act__points { position: relative; z-index: 1; display: grid; gap: .55rem; margin-bottom: 1.6rem; }
    .act__points li { display: flex; gap: .8ch; align-items: flex-start; font-size: var(--step--1); color: var(--read); }
    .act__points li::before { content: ""; flex: 0 0 7px; height: 7px; margin-top: .5em; background: var(--red); }
    .act__cta { position: relative; z-index: 1; display: flex; flex-wrap: wrap; gap: 1rem; }
  </style>

  <script is:inline>
    document.documentElement.classList.add('fx');
    (function () {
      var off = function () { document.documentElement.classList.remove('fx'); };
      setTimeout(off, 2500);
      addEventListener('error', off);
      addEventListener('unhandledrejection', off);
    })();
  </script>
</head>

<body data-page="activites">
  <div class="grain" aria-hidden="true"></div>
  <div class="vignette" aria-hidden="true"></div>
  <div class="cursor" aria-hidden="true"><div class="cursor__ring"><span class="cursor__label">voir</span></div></div>
  <div class="cursor__dot" aria-hidden="true"></div>

  <a class="sr-only" href="#main">Aller au contenu</a>
  <div id="nav"></div>
  <div id="drawer"></div>

  <main id="main">

    <!-- ========================= EN-TÊTE ========================== -->
    <section class="page-head" aria-label="Les activités">
      <div class="page-head__bg" aria-hidden="true"><div class="media" data-img="/assets/img/bc/training-2.webp" data-eager data-label=""></div></div>
      <div class="wrap">
        <span class="eyebrow" data-reveal>Les activités · Les Minimes</span>
        <h1 class="display" style="margin:1rem 0 0"><span class="reveal-mask"><span>Tout ce qui</span></span><span class="reveal-mask"><span class="tint">se boxe ici.</span></span></h1>
        <p class="lead page-head__lead" data-reveal>Six disciplines. Un seul geste au bout : le tien. Tu choisis la porte, on te montre le mouvement. Le reste, c'est du travail.</p>
      </div>
    </section>

    <!-- ========================= SOMMAIRE ========================= -->
    <nav class="wrap act-index" id="act-index" aria-label="Sommaire des disciplines"></nav>

    <!-- ====================== LES DISCIPLINES ===================== -->
    <section class="section section--tight" aria-label="Le catalogue des disciplines">
      <div class="wrap act-rows" id="act-rows" data-reveal-group></div>
    </section>

    <!-- ========================= FINAL GONG ====================== -->
    <section class="gong" aria-label="Réserver une séance">
      <div class="gong__bg" aria-hidden="true"><div class="media" data-img="/assets/img/bc/anglaise-3.webp" data-label=""></div></div>
      <div class="gong__inner">
        <span class="eyebrow" data-reveal>Le choix</span>
        <h2 class="display" data-reveal style="font-size:var(--step-3)">Choisis-en une.<br><span class="tint">Le coach fait le tri.</span></h2>
        <p class="lead" data-reveal>Tu changes d'avis en cours de route ? Ça arrive à tout le monde — la salle est ouverte six jours sur sept, tu essaies l'autre.</p>
        <div class="gong__cta" data-reveal>
          <a class="btn btn--primary" data-magnetic href="https://box-plus.vercel.app/seance-essai"><span>Réserver mon essai · 10€</span></a>
          <a class="btn btn--ghost" data-magnetic href="/plannings/"><span>Voir le planning</span></a>
        </div>
      </div>
    </section>

  </main>

  <div id="footer"></div>

  <!-- pastille "Une question ?" → appel direct de la salle (STANDARDS §1) -->
  <a class="chatbot" href="tel:+33562244682" aria-label="Une question ? Appelle la salle Boxing Center Minimes — 05 62 24 46 82">
    <span class="chatbot__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.36 1.9.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.9.34 1.85.57 2.81.7A2 2 0 0 1 22 16.92z"/></svg></span>
    <span class="chatbot__label">Appelle la salle</span>
  </a>

  <!-- Libs (CDN — la team remplace par des imports npm en Astro/Next) -->
  <script is:inline src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
  <script is:inline src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js"></script>
  <script is:inline src="https://cdn.jsdelivr.net/npm/@studio-freight/lenis@1.0.42/dist/lenis.min.js"></script>
  <script is:inline type="module" src="/assets/js/site.js?v=b5"></script>
  <script is:inline type="module" src="/assets/js/activites.js?v=b5"></script>
</body>
</html>
